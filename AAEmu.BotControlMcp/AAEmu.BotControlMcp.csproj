<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <AssemblyName>AAEmu.BotControlMcp</AssemblyName>
    <RootNamespace>AAEmu.BotControlMcp</RootNamespace>
    <Description>MCP sidecar over the contract-action API /api/actors/* (M5 stage 4, t_446228b5)</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reuse the P1 HTTP client (token auth, GET/POST) — no duplicated client. -->
    <ProjectReference Include="..\AAEmu.BotControl\AAEmu.BotControl.csproj" />
  </ItemGroup>
</Project>
